norm <- read.csv('normalized_dates.csv', delimiter='\t')
norm <- read.csv('normalized_dates.csv', sep='\t')
nrow(norm$norm.dates)
ncol(norm$norm.dates)
head norm
head(norm)
norm <- read.csv('normalized_dates.csv')
nrow(norm$norm.dates)
head(norm)
list(norm)
colnames(norm)
head(norm$norm.dates)
length(norm$norm.dates)
ncol(norm$norm.dates)
class(norm$norm.dates)
name='BMI'
norm$name
norm['name']
norm[name]
class(norm[name])
class(norm$norm.dates)
bmi <- as.numeric(norm[name])
fac <- as.factor(norm[name])
norm[name]
bmi <- sapply(norm[name], as.factor)
bmi
class(bmi)
length(bmi)
length(norm.dates)
length(norm$norm.dates)
class(norm$norm.dates)
bmi <- sapply(norm[name], as.numeric)
bmi
plot(norm$norm.dates, bmi)
bmi <- sapply(norm[name], as.numeric)
